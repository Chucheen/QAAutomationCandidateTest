<div class="container" style="text-align: center">
  <div class="row">
      <div class="div-scaffold span6 offset3">


        <p id="notice"><%= notice %></p>

                            <p>
              <strong>Order no:</strong>
              <%= @order.order_no %>
            </p>

                    <p>
              <strong>Customer:</strong>
              <%= @order.Customer_id %>
            </p>

                    <p>
              <strong>Total:</strong>
              <%= @order.total %>
            </p>

                    <p>
              <strong>Date:</strong>
              <%= @order.date %>
            </p>

              </div>
  </div>
  <%= link_to t('scaffold.edit'), edit_order_path(@order), class:"scaffold btn btn-large btn-primary" %>
  <%= link_to t('scaffold.back'), orders_path, class:"scaffold btn btn-large btn-primary" %>
</div>